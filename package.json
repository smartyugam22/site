{
  "name": "smart-yugam-academy-site",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "npm start -w frontend",
    "build": "npm run build -w frontend",
    "test": "npm test -w frontend",
    "eject": "npm run eject -w frontend"
  },
  "engines": {
    "node": "22.x"
  }
}
